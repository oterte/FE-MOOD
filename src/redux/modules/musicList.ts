
const initialState = {
  musicFile: '',
  musicTitle: '',
  musicComposer: '',
}

const musicPlayer = (state = initialState, action: any) => {
    switch (action.type) {
        default: 
            return state;
    }
}

export default musicPlayer;